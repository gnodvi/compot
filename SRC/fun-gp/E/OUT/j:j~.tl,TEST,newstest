
TEST_test_NEW ...

3
3 9
8 4

A A B
A A B B
A B A B

8 4
{4 2}

5.1

{5.0 3.0}
{2.0 -2.0}

-----------------------------------

0

4

-----------------------------------

0 
debug, stack = asas
debug, stack = asas sdds
asas sdds 

-----------------------------------

debug, stack = 1 null succ {dup pred} *
debug, stack = 1
debug, stack = 1 null
debug, stack = 1 null succ
debug, stack = 1 null succ {dup pred}
debug, stack = 1 null succ {dup pred} null succ {dup pred} *
debug, stack = 1 null succ {dup pred} null succ {dup pred} *
debug, stack = 1 null succ {dup pred} {null succ {dup pred} *}
debug, stack = 1 null succ {dup pred} {null succ {dup pred} *} linrec
debug, stack = 1 null succ {dup pred} {null succ {dup pred} *} linrec *
debug, stack = 1 null succ {dup pred} {null succ {dup pred} *} {linrec *}
debug, stack = 1 null succ {dup pred} {null succ {dup pred} * linrec *}
debug, stack = 1 null succ {dup pred null succ {dup pred} * linrec *}
 

-----------------------------------


