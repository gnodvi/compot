# -*-   mode: text  coding: koi8   -*- -----------------------------------------


Общие положения:

- квантовые вычисления это интересно, но не доказано, что обычными
  алгоритмами нельзя решить те же задачи!

- возможно, что более перспективны "однонаправленные" вычисления, т.е.
  квантовый отжиг и задачи поиска оптимума.



Конкретные шаги по развитию:

myliby_qvm : пример квантовй алгебры низкого уровня, как раз для квантового 
             отжига?
   - надо пересобрать для новой версии libquantum 1.1.1, там вместо ссылки на 
     структуру сама структура:
   - оставить только несколько тестов и интегрировать как пример в саму
     библиотеку mylibq.

myliby : сделать дополнительные тесты, рефакторинг и переводить
         в tcl/tk, интегрируясь с "compot/O/numer/q-ubit.tl"

Также можно что-то добавить, по Phyton, например от IBM.

# ------------------------------------------------------------------------------